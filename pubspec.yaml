name: mobile
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:            # 🆕 обязательно
    sdk: flutter
  dio: ^5.0.0
  go_router: ^14.8.0
  flutter_secure_storage: ^8.0.0
  pin_code_fields: ^8.0.1
  intl_phone_field: ^3.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  provider: ^6.0.5
  cached_network_image: ^3.2.0
  url_launcher: ^6.3.1
  shimmer: ^3.0.0
  intl: ^0.20.2
  geolocator: ^14.0.2
  flutter_rating_bar: ^4.0.1
  cupertino_icons: ^1.0.8
  google_sign_in: ^7.2.0

  flutter_map: ^8.2.2
  map_launcher: ^4.4.2
  latlong2: ^0.9.1
  flutter_svg: ^2.2.3
  share_plus: ^12.0.1
  image_picker: ^1.2.1

  package_info_plus: ^8.0.0
  flutter_markdown: ^0.7.7+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.0
  build_runner: ^2.1.7

  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/infogo_logo.webp
    - assets/infogo_logo.png
    - assets/flags/uz.png
    - assets/icons/google.png

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/info_go_advanced.png"
  remove_alpha_ios: true